[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd backend && node src/server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[deploy.healthcheck]]
type = "http"
path = "/api/auth/login"
